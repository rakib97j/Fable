import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us | Fable Support & Inquiries",
  description:
    "Get in touch with the Fable team for author publishing inquiries, reader support, billing, or general feedback.",
};

export default function ContactPage() {
  return <ContactClient />;
}
