import React from 'react';
import WritersClientPage from './WritersClientPage';

export const metadata = {
  title: 'Writers & Authors | Fable',
  description: 'Explore talented authors and independent writers publishing on Fable.',
};

export default function Page() {
  return <WritersClientPage />;
}
